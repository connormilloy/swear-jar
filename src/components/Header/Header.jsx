import STYLES from './Header.module.css';
const Header = () => {
    return (
        <header className={STYLES.Header}>
            <h1 className={STYLES.Title}>Swear J*r</h1>
        </header>
    )
}

export default Header;